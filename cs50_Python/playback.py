# implement a program in Python that prompts the
# user for input and then outputs that same input,
# replacing each space with " ... "


playback_speed = input("--> ")
playback_speed = playback_speed.replace(" ", "...")
print(playback_speed)
